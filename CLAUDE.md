# Life Crystal — คู่มือโปรเจกต์สำหรับ Claude

## โหมด: สั่งงานด้วยเสียงภาษาไทย

ผู้ใช้โปรเจกต์นี้สั่งงานด้วย **การพิมพ์ด้วยเสียงภาษาไทย (Win + H)** เป็นหลัก
ข้อความที่ได้รับจึงมักเป็นข้อความที่ถอดจากเสียงพูด ไม่ใช่ข้อความที่พิมพ์มาอย่างประณีต

### สิ่งที่ต้องคาดไว้เสมอ

1. **ไม่มีวรรคตอน / ไม่มีเว้นวรรค** — ให้ตัดคำเองตามบริบท อย่าถามเพราะเหตุนี้
2. **ศัพท์เทคนิคถูกถอดเป็นอักษรไทย** — ดูตารางเทียบคำด้านล่าง
3. **คำซ้ำ คำติดอ่าง คำเกิน** เช่น "เอ่อ" "คือว่า" "อ่าาา" — ให้ตัดทิ้ง
4. **ประโยคยาวต่อกันเป็นพืด** — อาจมีหลายคำสั่งในข้อความเดียว ให้แยกเป็นงานย่อยแล้วทำตามลำดับ
5. **ตัวเลข/ชื่อไฟล์อาจเพี้ยน** — ถ้าเดาได้จากไฟล์จริงในโปรเจกต์ ให้เดาแล้วบอกว่าเดาอะไร

### ตารางเทียบคำ (เสียงไทย → สิ่งที่หมายถึงจริง)

| ได้ยินเป็น | หมายถึง |
|---|---|
| เด็คบิลเดอร์ / เด็กบิวเดอร์ | `deck_builder_logic.js`, `deck_builder.css` |
| แบนลิสต์ / แบนลิส | `banlist_data.js` |
| การ์ดสแตท / การ์ดสเตท | `card_stats.js` |
| การ์ดดาต้า | โฟลเดอร์ `card_data/` |
| แคลน / คลาน / เคลน | clan — `clan_identity_logic.js`, `clan_dp_icons.js/.css` |
| ควิกแอด / ควิกแอ๊ด | `quickadd_logic.js`, `quickadd.css` |
| โชว์เคส | `Showcase_logic.js`, `showcase_mode.css` |
| แฮนด์ซิม / จำลองมือ | `handsimulate_logic.js` |
| อันแท็ป / อันแทป | `untap_logic.js` |
| เมนเจเอส / เมน | `main.js` |
| อินเด็กซ์ / หน้าแรก | `index.html` |
| รีสปอนซีฟ | `responsive.css` |
| ฟิลเตอร์ | `filterdiv.css` |
| เซฟเด็ค | `save_deck_logic.js` |
| คอลเลกชัน | `collection_logic.js`, `collection_style.css` |
| เอไอเด็ค | `ai_deck.js` |
| แคปเจอร์ | `capturelogic.js` |
| ไดนามิกบีจี / พื้นหลัง | `dynamic_bg.js` |
| คำศัพท์ / คีย์เวิร์ด / อภิธานศัพท์ | `keyword_glossary.js` (ข้อมูล), `ability_enhance.js` (popup + ชื่อการ์ดกดค้นหา) |
| ฟอร์แมตเด็ค / ตรวจเด็ค / กฎจำนวนการ์ด | `deck_format.js` |
| ลิงก์เด็ค / โค้ดเด็ค / แชร์เด็ค | `deck_code.js` |
| ใบลงทะเบียน | `deck_register.js` |
| การ์ดที่ขาด / การ์ดที่มี | `showcase_owned.js` |
| สไตล์ฟีเจอร์ใหม่ | `lc_features.css` |
| ปุ่มกลับการ์ดเดิม / ย้อนกลับค้นหา | `search_trail.js` |
| โหมดจัดเด็คแบบย่อ / เมนูสามจุด | `build_compact.js`, `build_compact.css` |
| สำรองข้อมูล / แบ็คอัพ / กู้คืน | `backup_all.js` |
| มีอะไรใหม่ / ปุ่มฟีเจอร์ใหม่ | `whats_new.js` |
| ข้อมูลเสีย / พื้นที่เต็ม / สตอเรจ | `lc_storage.js` |
| ค้นหาไม่เจอ / เตือนปิดแท็บ / วันที่อัปเดตข้อมูล | `site_extras.js` (แก้วันที่ที่ `CARD_DATA_UPDATED`) |
| โน้ตเด็ค / ค้นหาเด็ค / ทำสำเนาเด็ค | `deck_extras.js` |
| การ์ดโปรด / ดูล่าสุด / ปัดการ์ด / ขนาดตัวอักษร / คีย์ลัด | `ux_extras.js` |
| บ็อททอมแนฟ / แถบล่าง | `bottom_nav.css` |
| ซีเอสเอส / สไตล์ | CSS |
| เจสัน / เจซัน | JSON |
| ฟังก์ชัน / ฟังชั่น | function |
| คอมมิท / คอมมิต | git commit |
| พุช / พุธ | git push |
| พูลรีเควส | pull request |
| บั๊ก / บัค | bug |
| คอนโซล | console |
| รีแฟคเตอร์ | refactor |
| เรสปอนสีฟ | responsive |

### คำสั่งควบคุมด้วยเสียง (ให้ตีความตามนี้เสมอ)

| ผู้ใช้พูดว่า | ให้ทำ |
|---|---|
| "ยกเลิก" / "ไม่เอาแล้ว" / "ลืมที่พูดไป" | ทิ้งคำสั่งล่าสุด ไม่ต้องทำต่อ แล้วรอคำสั่งใหม่ |
| "แก้เมื่อกี้เป็น ..." | แทนที่คำสั่งก่อนหน้าด้วยข้อความใหม่ |
| "ย้อนกลับ" / "อันดู" | คืนไฟล์กลับเป็นก่อนแก้ล่าสุด |
| "อ่านให้ฟัง" / "สรุปสั้น ๆ" | ตอบสั้น ๆ ไม่เกิน 3 ประโยค ไม่ต้องใส่ตาราง/โค้ดบล็อกยาว |
| "ทำต่อ" / "เอาเลย" | ดำเนินการตามแผนล่าสุดโดยไม่ต้องถามยืนยันซ้ำ |
| "หยุดก่อน" | หยุดทำงาน สรุปสถานะปัจจุบัน |

### กติกาการตอบ

- **ตอบเป็นภาษาไทย** เสมอ เว้นแต่ผู้ใช้เปลี่ยนไปพูดอังกฤษ
- **สรุปสั้น** — ผู้ใช้กำลังฟัง/อ่านเร็ว ไม่ต้องอธิบายโค้ดบรรทัดต่อบรรทัด บอกแค่ว่าแก้ไฟล์ไหน แก้อะไร
- **ถามให้น้อย** — ถ้าเดาเจตนาได้ระดับมั่นใจพอควร ให้ลงมือแล้วบอกว่า "เข้าใจว่า ... ถ้าไม่ใช่บอกได้"
  ถามเฉพาะตอนที่ทางเลือกต่างกันมากและย้อนกลับยาก
- **ก่อนแก้ไฟล์หลายไฟล์** ให้บอกรายชื่อไฟล์ที่จะแก้เป็นบรรทัดสั้น ๆ ก่อน 1 ครั้ง
- **อย่าถามซ้ำเรื่องคำผิดจากเสียง** — เดาแล้วระบุว่าเดา ดีกว่าถาม

## เกี่ยวกับโปรเจกต์

Life Crystal — เว็บแอปฝั่ง client ล้วน (HTML/CSS/JS ธรรมดา ไม่มี build step)
สำหรับสร้างเด็ค/ดูข้อมูลการ์ดของเกมการ์ด แก้ไฟล์แล้วเปิด `index.html` ดูผลได้เลย
